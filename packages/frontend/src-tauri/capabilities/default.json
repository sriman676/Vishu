{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window. The harness spawns the vishu core natively (std::process), no shell plugin.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

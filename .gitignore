# Node / pnpm
node_modules/
dist/
*.tsbuildinfo

# Logs / scratch
*.log
*.err
*.out
*.tmp

# Rust / Tauri build artifacts (large — never push)
packages/frontend/src-tauri/target/

# Python sidecars
__pycache__/
*.pyc
.venv/
venv/

# Secrets / local runtime state
.env
.env.*
!.env.example
core.token
.vishu/

# OS
.DS_Store
Thumbs.db

# §12b voice: downloaded whisper.cpp/piper binaries + models (one-time setup, never committed)
packages/core/sidecar/voice-models/
# Playwright MCP runtime profile/trace output
.playwright-mcp/
nim-models.json
keys-health.json

# /brag: keep the delivered mp4/jpg/share-copy, ignore the regenerable Hyperframes source
brag-output/composition/
brag-output/brag-plan.md
brag-output/composition-brief.md

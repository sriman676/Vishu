{
  "name": "vishu",
  "version": "0.0.0",
  "private": true,
  "description": "Vishu — local-first, provider-agnostic agent that builds software.",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "setup": "node setup.mjs",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "vishu": "node packages/core/dist/bin/vishu.js",
    "dev:app": "pnpm --filter @vishu/frontend dev",
    "build:app": "pnpm --filter @vishu/frontend build"
  },
  "dependencies": {
    "@vishu/core": "workspace:*"
  }
}
